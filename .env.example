# Camouflage target domain (must support TLSv1.3 + HTTP/2)
SNI=www.samsung.com

# Leave empty to auto-generate
SHORT_ID=

# Post-quantum cryptography (ML-KEM-768 + ML-DSA-65)
ENABLE_PQ=true
ENABLE_STATS=false

PROXY_PORT=443
PANEL_PORT=3000

# Leave empty to disable auth (user: admin)
PANEL_PASSWORD=

# Override auto-detected IP (for NAT / CDN)
SERVER_IP=
